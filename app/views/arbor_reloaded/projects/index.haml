.projects-dashbord.row
  - if @projects.count > 0
    .large-6.large-centered.columns
      %p.create-project= t('create_project.title')
      =link_to t('create_project.project_button'), new_project_path, class: 'button radius small success'
      - @projects.each do |project|
        %li
          = link_to project.name, arbor_reloaded_project_canvases_path(project_id: project.id), title: " Go to #{project.name}", class: 'sidebar-project-item'
          = link_to edit_project_path(project), id: 'icon_edit_project' do
            %span.icon-edit
  - else
    .no-projects
      %p.title= t('reloaded.project_dashboard.no_projects')
      %p= t('reloaded.project_dashboard.get_started')
      =link_to t('reloaded.project_dashboard.button'), new_project_path, class: 'button radius small modal-btn'
