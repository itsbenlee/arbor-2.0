#group-modal.reveal-modal{'aria-hidden' => 'true', 'aria-labelledby' => 'group-modal', 'data-reveal' => '', :role => 'dialog' }
  = link_to '×', '#', class: 'close-reveal-modal'
  = form_for new_group, url: arbor_reloaded_project_groups_path, html: { id: 'new_group' }, remote: true  do |f|
    %h2= t('reloaded.create_project.title')
    = f.text_field :name, placeholder: t('reloaded.create_project.project_name'), class: 'group-name radius login'
    .new-group-errors
    .action
      = f.submit t('reloaded.create_project.save_button'), class: 'button radius tiny', id: 'save-group'
