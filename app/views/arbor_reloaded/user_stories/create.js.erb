$('.backlog-story-input.role input').val('');
$('.backlog-story-input.role input').focus();
$('.backlog-story-input.action input').val('');
$('.backlog-story-input.result input').val('');
$('.user-stories-container').html("<%= j render 'arbor_reloaded/user_stories/user_stories_list', project: @user_story.project.reload, new_group: Group.new(project_id: @user_story.project.id)%>");
backlogGeneralBinds();
