$('.backlog-story-input.role input').val('');
$('.backlog-story-input.role input').focus();
$('.backlog-story-input.action input').val('');
$('.backlog-story-input.result input').val('');
$('.backlog-story-list').append('<%= j render(@user_story) %>');
autogrowInputs();
