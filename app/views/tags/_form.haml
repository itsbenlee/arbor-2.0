= form_for(tag, remote: true) do |form|
  = form.text_field :name, class: 'backlog-placeholder', placeholder: t('backlog.user_stories.new_tag')
  = form.submit t('save'), id: 'save-tag', class: 'hide'
